function judge(raw){
  if(!raw) return false
  if(typeof raw !== 'object') return false
  let res = false
  const keys = Object.keys(raw)
  keys.forEach()
}

const obj = {

}
obj.a = obj

console.log(judge(obj))